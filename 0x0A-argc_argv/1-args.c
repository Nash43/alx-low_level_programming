#include <stdio.h>
#include <stdlib.h>

/**
 * main: prints number of arguments passed on it
 * Return 0: sucess
 * argc: prints argument count
 * argv: prints argument vector
 */

int main(int argc, char *argv[])
{
	printf(" argc = %d\n", argc);
	return (0);
}
